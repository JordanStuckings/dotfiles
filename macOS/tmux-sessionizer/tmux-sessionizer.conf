# Override default search paths
TS_SEARCH_PATHS=(~/)

# Add your code directories with depth
# Format: path:depth
TS_EXTRA_SEARCH_PATHS=(
    ~/projects:3
)

# Or set a global max depth (applies to paths without :depth suffix)
TS_MAX_DEPTH=2
